function arrayFilter(sentence){
    let wordsInSentence = sentence.split(" ");
    console.log (wordsInSentence)
    var chars = sentence.length;
    console.log(chars);
    let wordsArray = [];
    console.log (wordsArray);
    for (var i=0; i<wordsInSentence.length;i++){
        console.log(wordsInSentence[i]);
        if (string.wordsInSentence[i].length > 3) {
            outputString += wordsInSentence[i] + " ";
            outputString.push
        }
            
    }
}

arrayFilter("London is a big city in the United Kingdom")
// should return "London city United Kingdom"
